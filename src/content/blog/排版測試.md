---
title: "排版測試頁面（勿發布）"
description: "純粹用來看標題、清單、表格、程式碼區塊等排版效果的填充內容，內容本身沒有意義。"
pubDatetime: 2026-07-15
draft: true
---

這是一段用來測試段落排版的填充文字。當文字量變多的時候，行高、字距、段落間距會不會讓閱讀變得吃力，是這篇測試想觀察的重點。Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.

再來一段比較短的，順便測試中英文混排的效果：This is a mixed 中英文 paragraph 用來看看 inline code 像 `const x = 42;` 或是連結 [範例連結](https://example.com) 在行內排版時的樣子，還有 **粗體文字**、*斜體文字*、***粗斜體***，以及 ~~刪除線文字~~ 混在一起會不會太擠。

## 這是二級標題 H2

以下用一段很長的文字測試長段落在容器寬度下的換行與可讀性。Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum. 這句話沒有任何意義，純粹是為了讓這個段落夠長，長到可以觀察在手機寬度、平板寬度、桌機寬度下分別會怎麼換行，以及中文字與英文字混排時的间距是否協調，還有數字 1234567890 混在中文裡面會不會特別突兀。

### 這是三級標題 H3

- 這是無序清單第一項
- 第二項，順便寫長一點看看清單項目換行之後，第二行會不會跟項目符號對齊
  - 巢狀清單第一層
  - 巢狀清單第二項
    - 再往下一層的巢狀清單
- 第三項，包含 `inline code` 跟 **粗體**

1. 有序清單第一項
2. 有序清單第二項
3. 有序清單第三項
   1. 巢狀有序清單
   2. 巢狀有序清單第二項

#### 這是四級標題 H4

> 這是一段引言（blockquote），用來看看引言的左邊框、縮排、字體顏色或斜體樣式在目前主題下長什麼樣子。
>
> 引言可以有多個段落，這是第二段，順便測試引言內的 **粗體** 與 [連結](https://example.com)。
>
> > 這是巢狀引言，測試多層引言的視覺區隔。

##### 這是五級標題 H5

```ts
// 這是一個 TypeScript 程式碼區塊，測試語法高亮
function greet(name: string): string {
  const message = `Hello, ${name}!`;
  console.log(message);
  return message;
}

interface User {
  id: number;
  name: string;
  active: boolean;
}
```

程式碼區塊後面接一段文字，測試上下間距是否合理。也可以用 inline code 測試 `npm install` 或 `git commit -m "message"` 這種指令。

###### 這是六級標題 H6

| 欄位 | 型別 | 說明 |
| --- | --- | --- |
| id | number | 主鍵 |
| name | string | 名稱，這格文字故意寫長一點看看表格會不會擠 |
| active | boolean | 是否啟用 |
| createdAt | Date | 建立時間 |

---

上面是一條水平分隔線，這段測試分隔線前後的間距。

![替代文字用來測試圖片說明的樣子](https://via.placeholder.com/800x400)

最後一段測試 task list（有些渲染器支援）：

- [x] 已完成的項目
- [ ] 未完成的項目
- [ ] 另一個未完成的項目

以下是一段刻意寫得很長很長的段落，重複填充內容，用來測試當單一段落文字量非常多的時候，整體閱讀節奏、行距、段落最大寬度（measure）會不會讓人覺得疲勞。Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. 這句話重複測試中文段落的長度：這是測試文字，這是測試文字，這是測試文字，這是測試文字，這是測試文字，這是測試文字，這是測試文字，這是測試文字，這是測試文字，這是測試文字。

## 結尾標題

測試結束，這篇文章 draft 設為 true，不會出現在正式的文章列表裡，但可以直接用網址 `/blog/_排版測試` 或本機預覽連結打開來看排版。
