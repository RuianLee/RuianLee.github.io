# 個人網站使用手冊

這份文件是給用 vibe coding 方式維護這個網站的自己看的：把問過 Claude 的問題跟解答，整理成好查的說明書。之後有新問題，直接在下面補新的一節就好，不用每次重問一次。

---

## Q1. 首頁的輪播圖（carousel）怎麼移到比較顯眼的位置？

**改了什麼**：原本輪播圖被放在「側欄＋最新文章」區塊的下面，畫面上要滑一段才看得到。已經把它移到 `<Header />` 正下方、最上面，變成首頁一打開就看到的主視覺。

**檔案位置**：[src/pages/index.astro](src/pages/index.astro)

- `id="hero-carousel"` 的 `<section>` 現在是 `<main>` 裡的第一個區塊（約在檔案 85 行附近）。
- 原本「側欄＋最新文章」的 `<div class="grid ...">` 往下移到輪播圖後面。
- 輪播的邏輯（上一張／下一張／小圓點）完全沒動，因為那段 `<script>` 是用 `#hero-carousel` 這個 id 去抓元素，不管它在 DOM 裡的哪個位置都找得到。

**輪播圖的內容從哪來**：`heroSlides`（在同檔案前段，`const heroSlides = allItems.slice(0, 3);`）— 直接抓「全部文章依日期排序後最新的 3 篇」，不用手動指定，新發一篇文章、日期最新，就會自動輪替進輪播。

---

## Q2. 每一篇文章的內容要怎麼改／新增？

文章都是 Markdown 檔案，放在 `src/content/<分類>/` 底下，例如：

- `src/content/blog/` — 文集
- `src/content/journal/` — 隨筆
- `src/content/faith/` — 靈修分享
- `src/content/morning-revival/` — 晨興生活
- `src/content/portfolio/` — 作品集

**改既有文章**：直接開對應的 `.md` 檔案編輯內文即可，例如 [src/content/blog/用-astro-建立個人網站的心得.md](src/content/blog/用-astro-建立個人網站的心得.md)。

**新增一篇文章**：在對應資料夾新增一個 `.md` 檔案，檔頭（frontmatter）至少要有：

```markdown
---
title: "文章標題"
description: "一兩句話的摘要，會顯示在列表跟輪播圖裡"
pubDatetime: 2026-07-14
---

這裡開始寫內文，一般 Markdown 語法都能用。
```

不同分類會多一些欄位（可以參考 [src/content.config.ts](src/content.config.ts) 裡對應分類的 `schema`）：

- `faith`（靈修分享）多了 `bookTitle`、`bookAuthor`（必填）
- `portfolio`（作品集）多了 `tags`（標籤陣列）、`url`（外部連結，選填）
- `blog`、`journal`、`morning-revival` 都有選填的 `modDatetime`（修改日期）

存檔後，開發伺服器（`npm run dev`）會自動偵測到新檔案並更新頁面，不用重啟。

**封面圖**：`coverImage` 欄位現在存的是 `src/assets/covers/` 底下的檔名（不含路徑、不含副檔名），例如 `coverImage: "我的文章"` 對應 `src/assets/covers/我的文章.jpg`，沒有對到檔案就自動 fallback 回灰色佔位色塊。詳細規則跟怎麼自動處理見 Q5。

> 上面「新增一篇文章」這幾步，現在也可以直接用 `npm run post` 這個 CLI 自動跑完，見 Q5。

---

## Q3. 大頭貼（首頁側欄的圓形頭像）要怎麼換成真的照片？

**現況**：[src/pages/index.astro](src/pages/index.astro) 第 152 行左右，側欄那個圓形頭像目前是純 CSS 畫的灰色圓圈，裡面顯示姓名的第一個字（`config.site.author.slice(0, 1)`），不是真的圖片。

```astro
<div class="media-placeholder text-foreground mx-auto flex size-20 items-center justify-center rounded-full text-2xl font-bold">
  {config.site.author.slice(0, 1)}
</div>
```

**要換成真實照片的步驟**：

1. 把照片放進 `src/assets/`，例如 `src/assets/avatar.jpg`（放 `src/assets/` 而不是 `public/`，Astro 會自動做圖片優化、產生適合的尺寸）。
2. 在檔案最上面加 `import avatar from "@/assets/avatar.jpg";` 以及 `import { Image } from "astro:assets";`。
3. 把上面那個 `<div>` 換成：

```astro
<Image src={avatar} alt={config.site.author} class="mx-auto size-20 rounded-full object-cover" width={80} height={80} />
```

同樣的邏輯，目前因為還沒有照片檔案，先記下步驟，等有照片再實際換上去。

---

## Q4. 首頁上方（輪播圖跟側欄之間）感覺有點空，可以怎麼加東西？

目前結構是：輪播圖 → 側欄（頭像＋分類＋標籤）／最新文章列表 → 分類卡片區。整體是「一個主視覺 + 一個列表頁」的組合，比較安靜、不擁擠，但確實資訊量偏少。幾個可以考慮的方向：

- **側欄加一小段自我介紹／近況**：目前側欄只有姓名 + 一句 `site.description`，可以加 2-3 句「最近在忙什麼」，讓人一打開網站更有「這是誰、現在在做什麼」的感覺。
- **輪播圖下方加一排數字統計**：例如「文集 X 篇・隨筆 Y 篇・晨興記錄 Z 篇」，用側欄「分類」區塊已經有的 `count` 資料就能做，不用額外資料來源。
- **加一個精選文章／置頂文章區**：跟「最新文章」（依時間排序）分開，手動挑 1-2 篇代表作放在最上面，例如在 frontmatter 加 `featured: true`（`blog` 分類其實已經有這個欄位但目前沒用到，見 [src/content.config.ts](src/content.config.ts) 第 16 行）。
- **加社群／聯絡方式的簡短說明文字**：目前側欄下面就是 GitHub 圖示，旁邊可以加一行「歡迎透過 GitHub 聯絡我」之類的引導文字，填一點留白。

這些都是錦上添花，不急著做；哪個方向感興趣可以再深入討論怎麼實作。

---

## Q5. 發布文章太麻煩（挑資料夾、對檔名、封面圖手動搬），有沒有自動化工具？

**背景**：Q2 提到的手動流程要做三件事：把 md 丟進對的 `src/content/<分類>/`、檔名要跟標題一致、封面圖要另外存、想加新分類還要同時改 `content.config.ts` 跟 `categories.ts`。這些現在都用 `npm run post` 這個 CLI 包起來了。

**封面圖新規則**：圖片統一放 `src/assets/covers/<代稱>.<副檔名>`，frontmatter 的 `coverImage` 只存 `<代稱>` 這個字串，跟文章標題／檔名完全無關，畫面渲染時由 [src/utils/getCoverImage.ts](src/utils/getCoverImage.ts) 解析回真正的優化圖片（`import.meta.glob` 掃 `src/assets/covers/*` 建對照表）。

- **重複使用同一張圖**：好幾篇文章的 `coverImage` 填一樣的代稱就好，例如都寫 `coverImage: "generic-tech"`，會指到同一個檔案，不用複製多份。也可以直接手動編輯 md 的 frontmatter 填代稱，不一定要跑 CLI。
- **預設圖**：`coverImage` 留空時，如果 `src/assets/covers/default.<副檔名>` 存在就會自動當預設封面；還沒放這個檔案的話，維持顯示灰色佔位色塊（不會破圖）。想要有預設圖的話，把想用的圖片存成 `src/assets/covers/default.jpg`（或 `.png`/`.webp` 等）就好，不用改任何程式碼。
- CLI 的 `npm run post` 只是幫忙把來源圖片複製進 `src/assets/covers/`（用來源檔案自己的檔名當代稱）並填好欄位，通常不需要手動碰，但手動編輯 frontmatter＋自己把圖丟進 `src/assets/covers/` 完全等價、也可以。

### 用法一：`npm run post`（不帶參數）— 統一入口

只有這一個命令，所有跟「分類」有關的操作都在同一個選單裡選，不用另外記別的指令：

```
$ npm run post

選擇分類：
  1) 作品集 (portfolio)
  2) 文集 (blog)
  3) 隨筆 (journal)
  4) 靈修分享 (faith)
  5) 晨興生活 (morning-revival)
  6) + 新增分類
  7) 修改分類名稱
  8) 刪除分類
輸入編號: 
```

選 1-5（既有分類）或 6（新增分類後）會接著問文章內容、進入「建立新文章」流程；選 7、8 則是分類管理操作，做完就結束，不會接著問文章的事。

#### 建立新文章（選現有分類，或選 6 新增分類）

```
輸入編號: 2
文章標題: 我的新文章
描述 (description): 這篇文章在講什麼
發布日期 (pubDatetime, YYYY-MM-DD) (預設: 2026-07-15): 
封面圖（留空用預設圖；輸入已用過的代稱可重複使用，或輸入圖片路徑新增一張）: ~/Desktop/封面.jpg

已建立文章骨架：src/content/blog/我的新文章.md
去 IDE 把內文寫完就好。
```

跑完之後，去 IDE 打開印出來的路徑，把內文寫完存檔即可，`npm run dev` 會自動偵測更新。若分類是 `faith`，中間會多問 `bookTitle`、`bookAuthor`（這是唯一有額外必填欄位的分類）。

選 6「+ 新增分類」會先問 slug（英文 kebab-case）、導覽列文字、頁面標題、頁面副標、首頁側欄文字，然後自動：

- 在 `src/content/<slug>/` 建資料夾
- 在 [src/content.config.ts](src/content.config.ts) 加一行 `collection(...)` 定義（用跟 journal／morning-revival 一樣的共用 schema，沒有客製欄位）
- 在 [src/config/categories.ts](src/config/categories.ts) 加一筆分類定義
- 自動跑 prettier 定版格式

建完直接接著問文章標題，等於「新增分類＋寫第一篇文章」一次做完。

**限制**：只支援「沿用共用欄位」的分類。如果將來這個分類需要客製欄位（像 faith 的 `bookTitle`/`bookAuthor`、portfolio 的 `tags`/`url`），CLI 不會自動生成，要自己去 `content.config.ts` 幫該分類的 schema 手動 `.extend({...})`。

#### 選 7「修改分類名稱」

```
輸入編號: 7
選擇要修改的分類（自訂路由的分類不列在這裡）：
  1) 作品集 (portfolio)
  2) 隨筆 (journal)
  ...
輸入編號: 2

直接按 Enter 保留原值。
導覽列顯示文字（navLabel） (預設: 隨筆): 生活隨筆
分類頁標題（pageTitle） (預設: 隨筆): 
分類頁副標（pageDesc） (預設: 生活中的隨手記錄。): 
首頁側欄顯示文字（homeDesc） (預設: 隨筆): 
要順便改網址代稱／資料夾名稱嗎？目前是 "journal"，改了網址跟資料夾都會跟著變。 (y/N): n

已更新分類「生活隨筆」。
```

只是想改「顯示文字」（導覽列、分類頁標題等）的話，四個欄位直接按 Enter 跳過不用改的即可，最後網址代稱那題填 `n`，slug／資料夾都不會動，文章網址不會變。如果連網址代稱都想改，最後那題填 `y` 再輸入新 slug，CLI 會把 `src/content/<slug>/` 資料夾整個改名、同步改 `content.config.ts` 裡的變數名稱／路徑，文章網址會跟著變。

#### 選 8「刪除分類」

```
輸入編號: 8
選擇要刪除的分類（自訂路由的分類不列在這裡）：
  1) 作品集 (portfolio)
  2) 隨筆 (journal)
  ...
輸入編號: 2
確定要刪除分類「隨筆 (journal)」嗎？這會修改程式碼檔案。 (y/N): y
該分類目前有 3 篇文章，要一併刪除所有文章嗎？ (y/N): n
要不要先備份這些文章？ (Y/n): y

文章已備份到：.post-backups/journal-1720999999999
已刪除分類 "journal"。
```

流程：

1. 二次確認要不要真的刪。
2. 問要不要連文章一起刪：
   - 填 **y** → 分類定義跟 `src/content/<slug>/` 底下所有文章一起刪掉，刪不回來（沒有備份）。
   - 填 **n** → 文章不會被刪，只會問要不要備份：
     - 備份 → 整個資料夾搬到 `.post-backups/<slug>-<時間戳記>/`（這個資料夾有加進 `.gitignore`，純本機保險用，之後找回來要自己去翻，或用 `git log` 找回原本的 commit）。
     - 不備份 → 文章留在原地 `src/content/<slug>/`，但分類定義已經移除，網站不會再讀取這個資料夾（等於「軟刪除」，之後想要復原就是重新建一個同名分類）。
3. 分類定義（`content.config.ts` + `categories.ts`）一定會被移除，差別只在文章去哪。

**選 7、8 共同的限制**：都只支援「沿用共用欄位」、非自訂路由的分類——有客製欄位的分類（faith、portfolio）或自訂路由的分類（blog）CLI 都拒絕自動處理，選單裡也不會列出來，要手動改。

### 用法二：`npm run post -- <檔案路徑>` — 已經在別的地方寫好 md，要歸檔

適合平常習慣先在別處（例如 Desktop、Notes 匯出）寫好整篇文章，再處理發布這件事的情況。

```
$ npm run post -- ~/Desktop/草稿.md
```

流程：

1. 讀取 frontmatter，檢查 `title` / `description` / `pubDatetime` 是否都有填（faith 分類還會檢查 `bookTitle` / `bookAuthor`）。**缺欄位就直接中止**，不會幫忙腦補內容——回去補齊 md 再重跑一次。
2. 問分類：草稿檔案的上層資料夾名稱如果剛好對到既有分類（例如檔案放在 `.../blog/草稿.md`），會直接建議用那個分類；不然跳出跟用法一一樣的分類選單（含「+ 新增分類」）。
3. 處理封面圖：
   - frontmatter 已有 `coverImage`，而且那個值已經是 `src/assets/covers/` 裡存在的代稱 → 不動它（本來就是有效的重複使用）
   - `coverImage` 指向一個本機圖片路徑（還沒搬進 `src/assets/covers/`）→ 自動複製進去（用來源檔名當代稱），並把欄位改寫成新代稱
   - 完全沒有 `coverImage` → 問一次要不要補（可以直接輸入已有代稱重複使用、或輸入圖片路徑新增一張，留空就跳過、之後 fallback 到預設圖／灰色色塊）
4. 檔名固定用「標題」重新算 slug（不沿用原始檔名），搬到 `src/content/<分類>/<slug>.md`，原始檔案會被移走、不留副本。

> 這個流程的分類選單目前是簡化版（只有既有分類 + 新增分類），沒有修改／刪除分類的選項——那兩個操作請跑 `npm run post`（不帶參數）走用法一的選單。

### 底層檔案結構（交接用，之後要改邏輯去這裡找）

```
scripts/
  post.ts                — 進入點，判斷有沒有帶路徑參數決定跑「新建（含分類管理選單）」還是「發布」流程
  post-lib/
    prompts.ts            — 互動問答（readline + line queue，避免批次貼上答案時漏字）
    categories.ts          — 分類選單（runCategoryMenu，統整選分類/新增/改名/刪除）＋改寫 content.config.ts、categories.ts 的邏輯
    coverImage.ts           — 複製圖片進 src/assets/covers/
    frontmatter.ts            — frontmatter 讀取（gray-matter）／手刻產生／局部欄位改寫
    validate.ts                — 必填欄位檢查表（title/description/pubDatetime + faith 的兩個欄位）
    paths.ts                    — 專案內各路徑常數
```

### 常見狀況

- 檔名衝突（同標題的檔案已存在）：`new` 流程會直接報錯要你換標題，不會覆蓋既有檔案。
- 圖片格式：只接受 `.jpg` `.jpeg` `.png` `.webp` `.avif` `.gif`。
- CLI 的欄位檢查只是「夠用」等級，不是完整複刻 Zod schema 的規則；真正驗證還是要跑一次 `npm run build`（裡面含 `astro check`）。

---

## Q6. 首頁「最新文章」列表的卡片封面圖，大小要怎麼調？

**檔案位置**：[src/components/Card.astro](src/components/Card.astro)

桌面版（`sm:` 以上）的卡片是用「絕對定位＋置中」做出來的：封面圖是獨立於文字欄位的一個置中小方塊，不是撐滿整張卡片左側。跟大小有關的設定分散在 4 個地方，彼此會互相牽動，改的時候要一起看：

**1. 照片本身的顯示尺寸**——`<Image>`（有封面圖，約第 41 行）跟旁邊 fallback 用的灰色色塊 `<div>`（沒封面圖，約第 46 行）上的 `sm:h-* sm:w-*`：

```astro
<!-- 有封面圖 -->
<Image ... class="... sm:aspect-square sm:h-60 sm:w-60" />

<!-- 沒封面圖時 fallback 的灰色色塊 -->
<div class="... sm:aspect-square sm:h-52 sm:w-60" />
```

放大/縮小照片本身，就是改這兩組 `h-*` / `w-*`。**兩邊要填一樣的數字**，不然「有封面圖」跟「沒封面圖」的卡片看起來大小會不一樣（目前這兩行的數字不一致，是測試過程留下的，記得同步）。想維持正方形，同一組裡 `h-*` 跟 `w-*` 也要填一樣的數字——如果高寬分開指定不同數字，`aspect-square` 會被蓋掉，變成長方形而不是正方形。

**2. 照片外層「畫框」的寬度上限**——包住 `<Image>` / 灰色色塊的那層 `<div>`（約第 35 行、第 45 行）上的 `sm:w-*`：

```astro
<div class="... sm:absolute sm:inset-y-0 sm:left-0 sm:flex sm:w-55 sm:items-center sm:justify-center">
```

這是照片可以置中活動的「畫框」寬度，必須**大於等於**第 1 點的照片寬度，不然照片會比畫框還寬，溢出去疊到右邊的文字欄位。

**3. 卡片整體高度上限**——`<li>`（約第 32 行）上的 `sm:h-*`：

```astro
<li class="... sm:relative sm:h-70">
```

邏輯一樣：照片高度不能超過「卡片高度 − 上下留白」，不然會跟下一張卡片的分隔線擠在一起，甚至蓋住下面的「閱讀更多」按鈕。

**4. 文字欄位的左邊留白**——內文 `<div>`（約第 50 行）上的 `sm:pl-*`：

```astro
<div class="min-w-0 overflow-hidden sm:pl-60">
```

要跟第 2 點的畫框寬度同步：`pl-* 的數字 = 畫框寬度數字 + 4`（4 代表 1rem，是照片跟文字中間的間距）。畫框變寬變窄，這裡沒跟著改，文字就會被照片蓋到、或留下不必要的空白。

**調整順序建議**：先改第 1 點的照片尺寸 → 如果超過第 2 點的畫框寬度就把畫框也放大 → 畫框變了，第 4 點的文字留白同步加 4 → 照片變高的話，回頭檢查第 3 點的卡片高度夠不夠。改完存檔，`npm run dev` 開著的分頁會自動重整，直接在瀏覽器上看效果、反覆調整到滿意的比例即可。

---

## Q7. 首頁標題、導覽列下面的分界線、輪播卡片內距，要怎麼調？

**改了什麼**：[src/components/Header.astro](src/components/Header.astro)、[src/components/HeroCarousel.astro](src/components/HeroCarousel.astro)（輪播圖後來被拆成獨立元件，首頁 [src/pages/index.astro](src/pages/index.astro) 現在只是 `<HeroCarousel slides={heroSlides} />` 一行帶進去）

- **站名標題放大**：Header 裡站名 `<a>` 的字級從 `text-3xl sm:text-4xl` 改成 `text-4xl sm:text-5xl`，旁邊的羽毛圖示也同步從 `h-7 sm:h-8` 放大成 `h-8 sm:h-9` 維持比例。標題本來就有 `text-center` 置中，不用另外調。
- **導覽列下面不要再框起來**：原本 `<nav id="nav-menu">` 用 `border rounded-xl`（四邊都有邊框、圓角，看起來像個方框），改成只留 `border-y`（上下兩條線），視覺上變成單純的分隔線，不會把左右兩側包起來。
- **輪播卡片文字太擠**：`HeroCarousel.astro` 裡輪播卡片內層白色區塊的 `max-w-lg` 改成 `max-w-2xl`，桌面版內距 `sm:p-10` 改成 `sm:p-12`，標題跟說明文字有更多呼吸空間，不會擠成好幾行。

**怎麼再調整**：直接改上面這幾個 class 裡的 Tailwind 數值（字級 `text-*`、寬度 `max-w-*`、內距 `p-*`，數字/字級越大代表越大），改完存檔 `npm run dev` 的分頁會自動重整，直接在瀏覽器上看效果。

---

## Q8. 輪播圖切換太生硬（直接跳），還有想要自動輪播，怎麼做？

**改了什麼**：[src/components/HeroCarousel.astro](src/components/HeroCarousel.astro)

原本輪播只有手動點左右箭頭／圓點才會切換，而且切換方式是用 `hidden` class 直接顯示／隱藏，沒有動畫。現在改成：

1. **自動播放**：每 5 秒自動換下一張（`<script>` 裡的 `AUTOPLAY_INTERVAL_MS = 5000`，單位毫秒）。滑鼠移到輪播圖上會暫停，移開後繼續；手動點箭頭／圓點切換時也會重新計時，不會切完馬上又被自動播放蓋掉。
2. **切換動畫改成淡入淡出（crossfade）**：不是直接跳過去，是舊的那張慢慢淡出、新的同時淡入，過渡時間 0.7 秒，靠檔案最後 `<style>` 區塊裡的 `.hero-slide { opacity; transition }` 控制。非目前顯示的投影片會加上 `inert` 屬性，滑鼠點不到、鍵盤 Tab 也不會跳進去。

**怎麼再調整**：

- 想改自動播放間隔：改 `AUTOPLAY_INTERVAL_MS` 這個數字（5000 = 5 秒）。
- 想改淡入淡出速度：改 `<style>` 裡 `.hero-slide` 的 `transition: opacity 0.7s ease;`，數字越大切換越慢越柔和。
- 使用者系統設定「減少動態效果」時，會自動關閉淡入淡出動畫直接切換，不用另外處理。

---

## Q9. 整個網站版面看起來比參考網站窄，怎麼調整寬度？

**背景**：原本 Header（頂部導覽列）跟 Body（首頁輪播圖、側欄、文章列表這些主要內容）用了兩個不同的寬度設定，Header 比較寬、Body 比較窄，兩邊對不齊，Body 看起來特別擁擠。

**改了什麼**：[src/styles/global.css](src/styles/global.css)

```css
@utility app-layout {
  @apply mx-auto w-full max-w-6xl px-4;   /* 頁面整體寬度：從 max-w-4xl 改成跟 Header 一樣寬的 max-w-6xl */
}

@utility max-w-app {
  @apply max-w-4xl;   /* 只控制「文章內文」的閱讀寬度，跟上面這個分開算，維持窄版方便閱讀 */
}
```

連帶把文章內頁（[src/pages/blog/[...slug]/index.astro](src/pages/blog/[...slug]/index.astro)、[src/pages/[category]/[...slug]/index.astro](src/pages/[category]/[...slug]/index.astro)）裡的正文 `<article>` 補上 `mx-auto`——因為正文故意維持窄版（用的是 `max-w-app`），版面整體加寬後如果沒置中，文字會整個貼左邊、右邊留一大塊空白。

**之後想再調整**：

- 想要整體版面更寬／更窄：改 `app-layout` 的 `max-w-6xl`（Tailwind 尺寸依序變寬：`max-w-4xl` < `5xl` < `6xl` < `7xl`）。改這裡記得 `header-layout`（同檔案裡、Header 專用的寬度設定）也要跟著改成一樣的值，Header 跟 Body 才會維持對齊。
- 想要文章正文行寬更寬／更窄（只影響閱讀體驗，不影響整體版面寬度）：改 `max-w-app`，這兩組設定互相獨立，改一個不會動到另一個。

---

## Q10. 輪播卡片可以做成毛玻璃（半透明＋模糊）效果嗎？

**檔案位置**：[src/components/HeroCarousel.astro](src/components/HeroCarousel.astro) 第 48 行左右，輪播卡片的白色區塊。

原本是 `bg-background`（完全不透明的背景色），改成：

```astro
<div class="bg-background/40 supports-backdrop-filter:backdrop-blur-md border-foreground/10 mx-auto max-w-2xl border p-8 text-center shadow-sm sm:p-12">
```

跟毛玻璃效果有關的是這三個 class：

- **`bg-background/40`**：`/40` 是不透明度（0~100），數字越小越透明。`bg-background` 是主題背景色變數，會自動跟著 light/dark 模式切換，不用另外處理深色模式。想更透明改更小的數字（例如 `/20`），想更明顯改更大（例如 `/60`）。
- **`supports-backdrop-filter:backdrop-blur-md`**：真正做出「毛玻璃」感的關鍵，會讓卡片背後的圖片模糊化透出來；只有透明度、沒有這個模糊效果，看起來只是普通的半透明色塊。強度由小到大：`backdrop-blur-sm` → `backdrop-blur`（預設）→ `backdrop-blur-md`（目前用的）→ `backdrop-blur-lg` / `backdrop-blur-xl`。`supports-backdrop-filter:` 前綴是保險，只有瀏覽器支援這個 CSS 特性才套用，避免極少數舊瀏覽器上卡片變半透明但完全沒模糊。
- **`border-foreground/10` + `border`**：加一條很淡的邊框（`/10` = 10% 不透明度），讓玻璃有一圈邊緣感，是可選的裝飾，不加也不影響毛玻璃效果本身。

**怎麼再調整**：改上面這幾個數字，存檔後 `npm run dev` 的分頁會自動重整，直接在瀏覽器上看效果、調整到滿意的透明度跟模糊程度即可。

---

## Q11. 頂部圖示太小、標題留白太多、分類頁標題橫幅想要滿版延伸、毛玻璃面板看起來不夠「灰」，要怎麼調？

**改了什麼**：[src/components/Header.astro](src/components/Header.astro)、[src/components/Socials.astro](src/components/Socials.astro)、[src/components/Main.astro](src/components/Main.astro)、[src/styles/global.css](src/styles/global.css)、[src/styles/theme.css](src/styles/theme.css)

**1. 頂部三個符號放大**：「關於」連結、社群圖示（GitHub）、深色模式切換鈕。

- `Header.astro` 最上面那條工具列，字級從 `text-sm` 改成 `text-base`（連帶讓「關於」變大），深色模式切換鈕從 `size-5` 改成 `size-6`（按鈕本身跟裡面的 `IconMoon`/`IconSunHigh` 要一起改，三處數字要一致，不然圖示會偏出按鈕範圍）。
- `Socials.astro` 裡圖示的 `size-6` 改成 `size-7`。
- 想再放大：同樣邏輯，Tailwind 的 `size-*` 數字越大圖示越大，`text-*` 字級同理（`text-sm` < `base` < `lg`）。

**2. 置中站名標題上下留白縮窄**：`Header.astro` 裡包住站名的 `<div>`，`py-8 sm:py-10` 改成 `py-4 sm:py-6`。想要更窄／更寬，直接改這兩個 `py-*` 數字。

**3. 分類頁標題橫幅（例如「作品集」「文集」那種置中標題＋副標的區塊）改成滿版延伸、不要左右框線**：這個橫幅其實是共用元件，[src/components/Main.astro](src/components/Main.astro) 裡 `sidebarCategories` 那個分支，套用到所有走 `Sidebar` 版型的分類頁（作品集/文集/隨筆/靈修分享/晨興生活/標籤頁）。

原本是一個有四邊框線＋圓角的置中卡片（`glass-panel rounded-2xl ... border`），卡片寬度被外層的 `max-w-6xl` 容器卡住，所以視覺上左右兩側會看到「框住」的邊界線。改法：

```astro
<div class="full-bleed glass-panel mb-10 border-x-0 py-8">
  <div class="app-layout text-center">
    <h1 class="text-2xl font-bold sm:text-3xl">{pageTitle}</h1>
    ...
  </div>
</div>
```

- `full-bleed` 是新加的共用 utility（定義在 `global.css`），靠 `position: relative; left: 50%; width: 100vw; transform: translateX(-50%)` 這組經典 CSS 技巧，讓元素的寬度／背景色突破外層 `max-w-6xl` 容器，延伸到視窗最左最右，不管外面套幾層容器都能用。之後其他地方想要「滿版延伸」的區塊（背景色、分隔線等），直接加這個 class 就好。
- `border-x-0` 把 `glass-panel` 原本四邊都有的邊框，取消左右兩邊，只留上下（`border-y` 效果），視覺上變成一條延伸到底的分隔線，不是一個「框」。拿掉 `rounded-2xl` 是因為滿版延伸後貼齊螢幕邊緣，圓角在那個位置看不出來、也不需要。
- 裡面多包一層 `app-layout`（跟 Header／其他內容同一個寬度的共用 utility），讓文字（標題、副標）維持跟頁面其他內容對齊的置中寬度，不會因為外層滿版就整排文字貼到螢幕最邊邊。

**4. 玻璃感面板（側欄頭像卡、分類卡、最新文章卡，以及上面第 3 點的標題橫幅）灰色調深、更看得出毛玻璃質感**：

一開始只調高 `glass-panel`（`global.css`）的背景不透明度（`bg-muted/55` → 想加深直接加大到 `/75`），但發現效果不明顯——因為淺色模式的 `--muted`（`#f7f7f6`）本來就跟 `--background`（`#fdfdfc`）幾乎同色，不透明度调再高、混出來的顏色還是接近純白，看不出「灰」。當時的做法是直接把 `--muted` 顏色本身調深（`#f7f7f6 → #e7e4de`）。

> **這個做法後來被推翻了**，見 Q13——把面板調成不透明的深灰色，反而讓它更像「實色卡片」而不是「玻璃」，質感上是退步。玻璃感真正需要的是「透」（低不透明度 + 強模糊）而不是「深」。`--muted` 現在維持 `#e7e4de` 這個值，但 `glass-panel` 已經改用別的邏輯調色，不再直接吃 `--muted`；這個色碼目前只影響「閱讀更多」按鈕之類的實色元件，不影響玻璃面板了。

---

## Q12. 大頭貼跟文章縮圖看起來很「平」，想要加陰影讓它們浮起來？

**改了什麼**：[src/styles/global.css](src/styles/global.css)、[src/components/Card.astro](src/components/Card.astro)、[src/components/Sidebar.astro](src/components/Sidebar.astro)

大頭貼原本就有陰影（`avatar-glass` utility），但顏色調深玻璃面板背景後對比變弱、不夠明顯，所以把陰影本身也加深加粗：

```css
/* global.css，avatar-glass */
box-shadow:
  inset 0 1px 1px color-mix(in oklab, white 55%, transparent),
  0 1px 0 color-mix(in oklab, white 25%, transparent),
  0 16px 32px -12px color-mix(in oklab, var(--foreground) 60%, transparent),  /* 原本 45%，加深 */
  0 6px 12px -6px color-mix(in oklab, var(--foreground) 35%, transparent);   /* 新加一層近距離的陰影，讓邊緣更立體 */
```

文章封面縮圖（作品集/文集等列表卡片、側欄「最新文章」小圖）原本完全沒有陰影，是純平面的圖片，另外加了一個新的共用 utility：

```css
/* global.css */
@utility photo-shadow {
  box-shadow:
    0 16px 32px -14px color-mix(in oklab, var(--foreground) 45%, transparent),
    0 4px 10px -6px color-mix(in oklab, var(--foreground) 25%, transparent);
}
```

套用在 [Card.astro](src/components/Card.astro)（列表卡片封面圖／無圖時的灰色佔位色塊）跟 [Sidebar.astro](src/components/Sidebar.astro)（最新文章小縮圖／佔位色塊），直接加 `photo-shadow` 這個 class 到 `<Image>` 或 `<div>` 上即可，不影響原本的圓角、`object-cover` 等設定。

**怎麼再調整**：

- 想要陰影更深／更淺：改 `color-mix(in oklab, var(--foreground) 45%, transparent)` 裡的百分比數字（越大陰影越黑越明顯）。
- 想要陰影範圍更大／更小、更擴散／更貼合：改前面兩個數字，第一個是垂直位移（往下多少）、第二個（帶負號）是模糊半徑，數字越大陰影越暈開、越遠。
- 之後其他圖片（例如首頁輪播圖以外的地方）想要一樣的浮起效果，直接在該元素加 `photo-shadow` 這個 class 就好，是共用的，不用重寫陰影參數。
- 改完存檔，`npm run dev` 開著的分頁會自動重整，直接在瀏覽器上比對效果。

---

## Q13. 玻璃感面板調深灰色之後還是不像玻璃、看起來髒髒的，到底問題在哪？

**背景**：Q11 第 4 點把 `glass-panel` 的背景改成不透明度 `/75` 的深灰色（`--muted: #e7e4de`），結果看起來更像「實色卡片」，完全沒有玻璃感，而且整體偏「髒」。

**根本原因**：玻璃感的本質是「透」，不是「深」。真正的毛玻璃（iOS 那種）需要三個條件同時成立：

1. **背景本身要夠亮、不透明度要夠低**——這樣才「透」得出東西。
2. **背後要有夠鮮明、沒有先模糊過的色塊**——玻璃是把背後的東西「模糊化」，如果背後本來就是一片均勻的淡灰（或已經被 `blur-3xl` 磨到幾乎看不出形狀），玻璃前後根本沒有視覺差異，模糊效果等於白做。
3. **要有亮邊／高光**——真正的玻璃邊緣會反光，一條淺色的內緣亮邊（inset highlight）＋白色邊框，是「這是一塊立起來的玻璃」跟「這是一塊灰色色塊」的關鍵差異。

**改了什麼**：[src/styles/global.css](src/styles/global.css)、[src/layouts/Layout.astro](src/layouts/Layout.astro)、[src/components/HeroCarousel.astro](src/components/HeroCarousel.astro)

**1. `glass-panel` 改成低不透明度背景 + `backdrop-filter: blur() saturate()`，並加白色內緣亮邊**：

```css
@utility glass-panel {
  position: relative;
  background: color-mix(in oklab, var(--background) 80%, transparent);
  border: 1.5px solid color-mix(in oklab, white 70%, transparent);
  box-shadow:
    inset 0 1px 1px color-mix(in oklab, white 80%, transparent),
    0 20px 40px -22px color-mix(in oklab, var(--foreground) 40%, transparent);

  @supports (backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px)) {
    background: color-mix(in oklab, var(--background) 48%, transparent);
    backdrop-filter: blur(24px) saturate(180%);
    -webkit-backdrop-filter: blur(24px) saturate(180%);
  }
}
```

- 背景改吃 `--background`（近白／近黑）而不是 `--muted`（灰色），不透明度也大幅降低（支援 `backdrop-filter` 的瀏覽器只有 48%），這樣才「亮」而不「濁」。
- `saturate(180%)`：這是讓玻璃「看起來高級」的關鍵，會把背後模糊的顏色飽和度拉高，讓透出來的色彩更鮮明、更有存在感，而不是灰濛濛一片。
- `border` 用白色半透明（`color-mix(in oklab, white 70%, transparent)`），呼應 `avatar-glass` 大頭貼原本就有的白邊——這是使用者實際指出「差在沒有白邊」後才確認的方向，讓玻璃面板有清楚的邊緣輪廓。
- `inset 0 1px 1px white` 在內緣頂部加一條高光，模擬玻璃反光。
- 不支援 `backdrop-filter` 的瀏覽器會 fallback 到不透明度 80% 的版本（沒有模糊效果的話太透明會看不清楚文字），用 `@supports` 判斷。

**2. 背景裝飾色塊調亮、少一點預先模糊，讓玻璃真的有東西可以模糊**：

```astro
<!-- Layout.astro -->
<div class="bg-accent/35 absolute -top-32 -left-24 size-120 rounded-full blur-2xl"></div>
<div class="bg-accent/25 absolute top-10 -right-32 size-136 rounded-full blur-2xl"></div>
<div class="bg-accent/18 absolute -bottom-40 left-1/4 size-112 rounded-full blur-2xl"></div>
<div class="bg-accent/20 absolute right-1/5 bottom-10 size-72 rounded-full blur-xl"></div>
```

原本這幾團色塊不透明度只有 `/10`～`/15`，而且用 `blur-3xl`（很重的模糊）——色塊本身就已經糊成一片，玻璃面板的 `backdrop-blur` 疊上去等於是「模糊疊模糊」，肉眼完全看不出差異。改成不透明度拉高到 `/18`～`/35`、模糊程度降到 `blur-2xl`/`blur-xl`，色塊本身仍然是柔和的光暈，但輪廓夠清楚，玻璃面板蓋上去時才看得出「喔，這裡把背後的顏色揉開了」的效果。

> **中間一度加過一團 `bg-muted-foreground/25` 的灰色色塊**，用意是增加層次，但因為 `muted-foreground` 是偏冷、偏濁的石灰色，跟其他暖色調的 `accent` 色塊放在一起，糊開後變成一塊視覺上很突兀的髒灰色斑塊，被直接抓出來說「很髒很醜」。**教訓：背景光暈色塊全部用同一個暖色系（`accent`）、只靠不透明度做深淺層次，不要混用冷色調的 `muted-foreground`，不然模糊之後顏色會互相髒污、看起來像沒洗乾淨。**

**3. 首頁輪播圖的說明卡片，統一改用 `glass-panel`**：[HeroCarousel.astro](src/components/HeroCarousel.astro) 原本自己寫了一套很像但不完全一樣的半透明模糊樣式（`bg-background/40 border-foreground/10 ...`），現在直接改成 `class="glass-panel mx-auto max-w-2xl p-8 text-center sm:p-12"`，跟側欄卡片、分類頁橫幅共用同一份玻璃樣式定義，以後调整質感只要改 `glass-panel` 一個地方，全站的玻璃元件會一起變。

**怎麼再調整**：

- 想要玻璃更清透／更不透明：改 `glass-panel` 裡 `color-mix(in oklab, var(--background) 48%, transparent)` 的百分比（數字是「不透明度」，越小越透明）。
- 想要模糊更強／更弱：改 `backdrop-filter: blur(24px)` 的數字。
- 想要背後的顏色更鮮豔／更淡：改 `saturate(180%)`，或改 `Layout.astro` 裡光暈色塊的不透明度（`/18`～`/35` 那些數字）。
- 之後想再加新的背景光暈色塊：**只用 `bg-accent/<數字>`**（暖色系），不要混用 `muted-foreground` 或其他冷色調，避免重演「糊出一塊髒灰斑」的問題。
- 改完存檔，`npm run dev` 開著的分頁會自動重整，直接在瀏覽器上比對效果。

> **這節的背景光暈色塊做法後來也被推翻了**，見 Q14——改用暖色調之後雖然不再是「髒灰色」，但色塊本身面積很大、模糊範圍很廣，實測起來整片背景還是偏暗濁，跟「整體要亮」的方向衝突。真正的問題不是色塊該用什麼顏色，而是「背景要不要放這種大面積色塊」這件事本身。

---

## Q14. 玻璃面板還是太髒，而且外框想要跟全站既有的虛線風格一致

**背景**：Q13 把背景光暈色塊全部統一成暖色調（`bg-accent`）之後，肉眼看還是覺得「濁」——根本原因是這個網站的 `--accent`（`#7c6650`，淺色模式）本身是低飽和度的咖啡棕色，不是鮮豔色。這種顏色只要面積夠大、模糊範圍夠廣，不管怎麼調不透明度，看起來都會像一片棕灰色的霧，而不是「發光」的感覺——**問題不是色塊該調亮還是調暗，是「頁面背景不該有這種大面積模糊色塊」本身**。同時，使用者也指出玻璃面板的白色實色外框跟全站既有的虛線視覺語言（導覽列底線、卡片「閱讀更多」按鈕、側欄分隔線都是 `border-dashed`）不一致。

**改了什麼**：[src/layouts/Layout.astro](src/layouts/Layout.astro)、[src/styles/global.css](src/styles/global.css)

**1. 直接移除背景裝飾光暈色塊**：`<body>` 裡原本那組 `pointer-events-none fixed inset-0 -z-10` 的色塊 `<div>`（Q13 加的）整組刪掉，回到單純的 `bg-background` 素色背景。玻璃感不再依賴「背後有沒有鮮豔色塊」，改成完全靠玻璃面板自己的材質（半透明背景＋模糊飽和度＋邊框＋高光陰影）撐起來——這對一個內容是文字為主、走「紙感／安靜」調性的個人網站來說也更合適，不需要靠花俏的背景光暈硬做出「科技感」。

**2. `glass-panel` 的外框改成虛線，不再是白色實色框**：

```css
@utility glass-panel {
  position: relative;
  background: color-mix(in oklab, var(--background) 85%, transparent);
  border: 1.5px dashed color-mix(in oklab, var(--muted-foreground) 45%, transparent);
  box-shadow:
    inset 0 1px 1px color-mix(in oklab, white 70%, transparent),
    0 20px 40px -22px color-mix(in oklab, var(--foreground) 30%, transparent);

  @supports (backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px)) {
    background: color-mix(in oklab, var(--background) 60%, transparent);
    backdrop-filter: blur(24px) saturate(180%);
    -webkit-backdrop-filter: blur(24px) saturate(180%);
  }
}
```

- `border` 從 `solid white` 改成 `dashed`、顏色改用 `--muted-foreground`（跟側欄分隔線 `border-muted-foreground/40 border-dashed` 同一套配色邏輯），呼應全站既有的虛線語言。
- 拿掉背景色塊後，玻璃面板背後可以透出的東西變少了，所以把不透明度稍微調高一點點補償可讀性／存在感（支援 `backdrop-filter` 時從 48% 提高到 60%），不然在素色背景上會太不明顯。
- `inset` 高光陰影維持，這是玻璃邊緣反光的關鍵，跟外框是虛線還是實線無關，繼續保留。
- **大頭貼 `avatar-glass` 的白色實色圓框沒有改**——那是使用者明確喜歡、要求保留的效果（見前面對話：「這種感覺很棒啊」），虛線化只套用在 `glass-panel`（側欄卡片、分類頁橫幅、輪播說明卡），跟大頭貼是兩個不同的視覺語言，不要搞混。

**這次調整之後的設計原則**（給以後想再調整玻璃感的自己）：

- **背景維持素色、亮**：不要再加大面積、高不透明度的裝飾色塊，這個網站的品牌色（`accent`）本身偏低飽和度，大面積模糊只會變成灰濁的霧，不會變成好看的光暈。
- **「玻璃感」完全靠面板自己的材質堆出來**：半透明背景（`background` + `backdrop-filter: blur() saturate()`）、虛線外框、內緣高光（`inset box-shadow`）、外部柔和陰影，四個一起上，不依賴背景要多花俏。
- 想要玻璃更清透／更不透明：改 `glass-panel` 裡 `color-mix(in oklab, var(--background) 60%, transparent)` 的百分比。
- 想要虛線更明顯／更淡：改 `border: 1.5px dashed color-mix(in oklab, var(--muted-foreground) 45%, transparent)` 的百分比或線寬。
- 改完存檔，`npm run dev` 開著的分頁會自動重整，直接在瀏覽器上比對效果。

---

## Q15. 版面想再更寬一點、側欄大頭貼想再放大，怎麼調？

**背景**：Q9 已經把 `app-layout`／`header-layout` 從 `max-w-4xl` 統一改成 `max-w-6xl`，這次是覺得還可以再寬一點，加上側欄大頭貼（[src/components/Sidebar.astro](src/components/Sidebar.astro)）比例上偏小，想跟着放大。

**改了什麼**：[src/styles/global.css](src/styles/global.css)、[src/components/Sidebar.astro](src/components/Sidebar.astro)

**1. 整體版面寬度**：`app-layout`、`header-layout` 這兩個 utility 的 `max-w-6xl` 一起改成 `max-w-7xl`（Tailwind 尺寸 `4xl` < `5xl` < `6xl` < `7xl`，數字越大越寬）：

```css
@utility app-layout {
  @apply mx-auto w-full max-w-7xl px-4;
}

@utility header-layout {
  @apply mx-auto w-full max-w-7xl px-4;
}
```

兩個一定要同步改成一樣的值，Header（導覽列）才會跟下面的內容對齊，道理跟 Q9 一樣。

**2. 側欄大頭貼放大**：`Sidebar.astro` 裡包住頭像的 `<div>`（有照片、跟沒照片時 fallback 顯示姓名首字的兩個版本都要改）尺寸從 `size-28` 改成 `size-40`：

```astro
<div class="avatar-blob avatar-glass mx-auto size-40">
  <img src={config.site.avatar} alt={config.site.author} class="size-full object-cover" />
</div>
```

**怎麼再調整**：

- 版面想再寬／窄：改 `app-layout`／`header-layout` 的 `max-w-*`，兩處記得同步。
- 頭貼想再大／小：改 `size-*` 數字，有照片跟沒照片（fallback）那兩個 `<div>` 要填一樣的數字，不然切換有無頭像時大小會跳動。
- 改完存檔，`npm run dev` 開著的分頁會自動重整，直接在瀏覽器上比對效果。

---

## Q16. 想幫背景加一點紙的質感，試了照片背景又太粗糙，最後怎麼定案的？

**背景**：想讓純白背景多一點溫度，中間試了三種做法，一個比一個更接近「紙」：

1. **真的紙張照片**（`background-image: url(paper.png)`，`background-size: cover` + `background-blend-mode: multiply` 讓照片跟 `--background` 混色）——能用，但照片有自然光影的明暗不均，直接鋪滿整頁在大螢幕上會看出「這是一張圖」，不是均勻的紙質感。後來又試了另一張紙質照片，結果更像水泥牆（有斑駁色塊跟裂紋），質感偏工業風，跟這個網站「靈修、文青、簡約」的調性不合，被排除。
2. **`radial-gradient` 疊小圓點**——純 CSS，不用圖檔，用 4 層不同尺寸／位置的 `radial-gradient` 疊出雜訊感。問題是圓點間距要拉大才能維持「幾乎看不到規律」，但間距一拉大，點跟點之間留白過多，近看反而像稀疏的髒污斑點，不是連續的紙纖維感觉，被使用者形容「粗糙」。
3. **SVG `feTurbulence` 雜訊**（目前定案的版本）——用 SVG 濾鏡的 `feTurbulence`（分形雜訊演算法）產生連續、無方向性的細顆粒，比圓點更接近真實紙纖維，肉眼看是平滑的雜訊而不是一顆顆分開的點。

**改了什麼**：[src/assets/paper-grain.svg](src/assets/paper-grain.svg)（新檔案）、[src/styles/global.css](src/styles/global.css)、[src/styles/theme.css](src/styles/theme.css)

一開始這個 SVG 是直接寫成一長串 URL-encode 過的字串塞進 `background-image: url("data:image/svg+xml,%3Csvg...")`，能動、但改一個數字要在編碼過的字串裡面找位置，很難維護（提出的原話：「這種改字串的方式很難管理」）。後來拆成兩塊：

**1. 雜訊圖案本身，搬進獨立的 `.svg` 檔案**——`src/assets/paper-grain.svg` 是一般的 SVG 檔，屬性都是明文（`baseFrequency="0.95"`），不用處理 URL 編碼，直接當普通檔案編輯：

```xml
<filter id="grain">
  <feTurbulence type="fractalNoise" baseFrequency="0.95" numOctaves="3" stitchTiles="stitch" result="noise" />
  <feColorMatrix in="noise" type="matrix" values="
    0 0 0 0 0.35
    0 0 0 0 0.28
    0 0 0 0 0.18
    0 0 0 1 0" />
</filter>
<rect width="100%" height="100%" filter="url(#grain)" />
```

- `baseFrequency`：顆粒粗細，數字越小顆粒越大越粗獷（像水泥），越大越細越像紙纖維。
- `numOctaves`：疊幾層不同頻率的雜訊，細節層次，2~4 之間差異不大，通常不用動。
- `feColorMatrix` 最後那組矩陣的前三行（`0.35`／`0.28`／`0.18`）：顆粒顏色（R G B），目前是跟 `--accent` 一致的暖棕色。第四行（`0 0 0 1 0`）是透明度直接沿用 `feTurbulence` 原始的雜訊值，不在這裡做縮放。

**2. 顆粒「明不明顯」，改用 CSS 變數控制**——`global.css` 裡不是把這個 SVG 直接設成 `body` 的 `background-image`，而是疊一層 `body::before` 偽元素：

```css
body {
  position: relative;
  ...
}
body::before {
  content: "";
  position: absolute;
  inset: 0;
  z-index: -1;
  background-image: url("../assets/paper-grain.svg");
  background-size: 300px 300px;
  opacity: var(--paper-grain-opacity);
  pointer-events: none;
}
```

`--paper-grain-opacity`（定義在 [src/styles/theme.css](src/styles/theme.css)，目前是 `0.35`）就是唯一需要調的數字。之所以要多包一層 `::before` 而不是直接在 `body` 上設 `opacity`，是因為 `opacity` 是整個元素（含所有子元素、文字內容）一起套用的效果——直接設在 `body` 上會連頁面內容一起變透明，所以顆粒的透明度必須用一個獨立、只蓋雜訊圖案本身的圖層來控制。

**怎麼再調整**：

- **想要更明顯／更淡**：只改 `theme.css` 裡的 `--paper-grain-opacity`（`0` ~ `1` 之間，數字越大越明顯），這是最常會動的一個數字，改這裡就好，不用碰 SVG。
- **想要顆粒更粗／更細**：改 `paper-grain.svg` 的 `baseFrequency`（數字越小越粗）。
- **想要顆粒換顏色**：改 `paper-grain.svg` 的 `feColorMatrix` 前三行數字（R G B，`0`~`1` 之間）。
- **想要顆粒的重複尺度更大／更小**：改 `global.css` 裡 `background-size: 300px 300px`（不建議低於 150px，太小容易在高密度螢幕上出現疊圖瑕疵）。
- 目前 light／dark 兩個主題共用同一個 `--paper-grain-opacity`，沒有另外為 dark 模式調低——因為這個透明度本身已經偏低，疊在近黑的 dark 背景色上實測幾乎看不出來，暫時不需要另外處理；如果之後想讓 dark 模式的顆粒更明顯或完全不同，可以在 `theme.css` 的 `[data-theme="dark"]` 區塊另外覆寫這個變數。
- 改完存檔，`npm run dev` 開著的分頁會自動重整，直接在瀏覽器上比對效果。

---

## 常用指令

```bash
npm run dev      # 本機開發伺服器，存檔即時預覽
npm run build    # 打包成靜態網站（部署前用來確認沒有錯誤）
npx astro check  # 檢查 Astro 檔案的型別／語法錯誤
npm run post     # 互動選單：建立新文章／新增／修改／刪除分類（帶檔案路徑則改成歸檔已寫好的 md，見 Q5）
```
