# 個人網站使用手冊

這份文件是給用 vibe coding 方式維護這個網站的自己看的：把問過 Claude 的問題跟解答，整理成好查的說明書。之後有新問題，直接在下面補新的一節就好，不用每次重問一次。

---

## Q1. 首頁的輪播圖（carousel）怎麼移到比較顯眼的位置？

**改了什麼**：原本輪播圖被放在「側欄＋最新文章」區塊的下面，畫面上要滑一段才看得到。已經把它移到 `<Header />` 正下方、最上面，變成首頁一打開就看到的主視覺。

**檔案位置**：[src/pages/index.astro](src/pages/index.astro)

- `id="hero-carousel"` 的 `<section>` 現在是 `<main>` 裡的第一個區塊（約在檔案 85 行附近）。
- 原本「側欄＋最新文章」的 `<div class="grid ...">` 往下移到輪播圖後面。
- 輪播的邏輯（上一張／下一張／小圓點）完全沒動，因為那段 `<script>` 是用 `#hero-carousel` 這個 id 去抓元素，不管它在 DOM 裡的哪個位置都找得到。

**輪播圖的內容從哪來**：`heroSlides`（在同檔案前段，`const heroSlides = allItems.slice(0, 3);`）— 直接抓「全部文章依日期排序後最新的 3 篇」，不用手動指定，新發一篇文章、日期最新，就會自動輪替進輪播。

---

## Q2. 每一篇文章的內容要怎麼改／新增？

文章都是 Markdown 檔案，放在 `src/content/<分類>/` 底下，例如：

- `src/content/blog/` — 文集
- `src/content/journal/` — 隨筆
- `src/content/faith/` — 靈修分享
- `src/content/morning-revival/` — 晨興生活
- `src/content/portfolio/` — 作品集

**改既有文章**：直接開對應的 `.md` 檔案編輯內文即可，例如 [src/content/blog/用-astro-建立個人網站的心得.md](src/content/blog/用-astro-建立個人網站的心得.md)。

**新增一篇文章**：在對應資料夾新增一個 `.md` 檔案，檔頭（frontmatter）至少要有：

```markdown
---
title: "文章標題"
description: "一兩句話的摘要，會顯示在列表跟輪播圖裡"
pubDatetime: 2026-07-14
---

這裡開始寫內文，一般 Markdown 語法都能用。
```

不同分類會多一些欄位（可以參考 [src/content.config.ts](src/content.config.ts) 裡對應分類的 `schema`）：

- `faith`（靈修分享）多了 `bookTitle`、`bookAuthor`（必填）
- `portfolio`（作品集）多了 `tags`（標籤陣列）、`url`（外部連結，選填）
- `blog`、`journal`、`morning-revival` 都有選填的 `modDatetime`（修改日期）

存檔後，開發伺服器（`npm run dev`）會自動偵測到新檔案並更新頁面，不用重啟。

**封面圖**：`coverImage` 欄位現在存的是 `src/assets/covers/` 底下的檔名（不含路徑、不含副檔名），例如 `coverImage: "我的文章"` 對應 `src/assets/covers/我的文章.jpg`，沒有對到檔案就自動 fallback 回灰色佔位色塊。詳細規則跟怎麼自動處理見 Q5。

> 上面「新增一篇文章」這幾步，現在也可以直接用 `npm run post` 這個 CLI 自動跑完，見 Q5。

---

## Q3. 大頭貼（首頁側欄的圓形頭像）要怎麼換成真的照片？

**現況**：[src/pages/index.astro](src/pages/index.astro) 第 152 行左右，側欄那個圓形頭像目前是純 CSS 畫的灰色圓圈，裡面顯示姓名的第一個字（`config.site.author.slice(0, 1)`），不是真的圖片。

```astro
<div class="media-placeholder text-foreground mx-auto flex size-20 items-center justify-center rounded-full text-2xl font-bold">
  {config.site.author.slice(0, 1)}
</div>
```

**要換成真實照片的步驟**：

1. 把照片放進 `src/assets/`，例如 `src/assets/avatar.jpg`（放 `src/assets/` 而不是 `public/`，Astro 會自動做圖片優化、產生適合的尺寸）。
2. 在檔案最上面加 `import avatar from "@/assets/avatar.jpg";` 以及 `import { Image } from "astro:assets";`。
3. 把上面那個 `<div>` 換成：

```astro
<Image src={avatar} alt={config.site.author} class="mx-auto size-20 rounded-full object-cover" width={80} height={80} />
```

同樣的邏輯，目前因為還沒有照片檔案，先記下步驟，等有照片再實際換上去。

---

## Q4. 首頁上方（輪播圖跟側欄之間）感覺有點空，可以怎麼加東西？

目前結構是：輪播圖 → 側欄（頭像＋分類＋標籤）／最新文章列表 → 分類卡片區。整體是「一個主視覺 + 一個列表頁」的組合，比較安靜、不擁擠，但確實資訊量偏少。幾個可以考慮的方向：

- **側欄加一小段自我介紹／近況**：目前側欄只有姓名 + 一句 `site.description`，可以加 2-3 句「最近在忙什麼」，讓人一打開網站更有「這是誰、現在在做什麼」的感覺。
- **輪播圖下方加一排數字統計**：例如「文集 X 篇・隨筆 Y 篇・晨興記錄 Z 篇」，用側欄「分類」區塊已經有的 `count` 資料就能做，不用額外資料來源。
- **加一個精選文章／置頂文章區**：跟「最新文章」（依時間排序）分開，手動挑 1-2 篇代表作放在最上面，例如在 frontmatter 加 `featured: true`（`blog` 分類其實已經有這個欄位但目前沒用到，見 [src/content.config.ts](src/content.config.ts) 第 16 行）。
- **加社群／聯絡方式的簡短說明文字**：目前側欄下面就是 GitHub 圖示，旁邊可以加一行「歡迎透過 GitHub 聯絡我」之類的引導文字，填一點留白。

這些都是錦上添花，不急著做；哪個方向感興趣可以再深入討論怎麼實作。

---

## Q5. 發布文章太麻煩（挑資料夾、對檔名、封面圖手動搬），有沒有自動化工具？

**背景**：Q2 提到的手動流程要做三件事：把 md 丟進對的 `src/content/<分類>/`、檔名要跟標題一致、封面圖要另外存、想加新分類還要同時改 `content.config.ts` 跟 `categories.ts`。這些現在都用 `npm run post` 這個 CLI 包起來了。

**封面圖新規則**：圖片統一放 `src/assets/covers/<代稱>.<副檔名>`，frontmatter 的 `coverImage` 只存 `<代稱>` 這個字串，跟文章標題／檔名完全無關，畫面渲染時由 [src/utils/getCoverImage.ts](src/utils/getCoverImage.ts) 解析回真正的優化圖片（`import.meta.glob` 掃 `src/assets/covers/*` 建對照表）。

- **重複使用同一張圖**：好幾篇文章的 `coverImage` 填一樣的代稱就好，例如都寫 `coverImage: "generic-tech"`，會指到同一個檔案，不用複製多份。也可以直接手動編輯 md 的 frontmatter 填代稱，不一定要跑 CLI。
- **預設圖**：`coverImage` 留空時，如果 `src/assets/covers/default.<副檔名>` 存在就會自動當預設封面；還沒放這個檔案的話，維持顯示灰色佔位色塊（不會破圖）。想要有預設圖的話，把想用的圖片存成 `src/assets/covers/default.jpg`（或 `.png`/`.webp` 等）就好，不用改任何程式碼。
- CLI 的 `npm run post` 只是幫忙把來源圖片複製進 `src/assets/covers/`（用來源檔案自己的檔名當代稱）並填好欄位，通常不需要手動碰，但手動編輯 frontmatter＋自己把圖丟進 `src/assets/covers/` 完全等價、也可以。

### 用法一：`npm run post`（不帶參數）— 統一入口

只有這一個命令，所有跟「分類」有關的操作都在同一個選單裡選，不用另外記別的指令：

```
$ npm run post

選擇分類：
  1) 作品集 (portfolio)
  2) 文集 (blog)
  3) 隨筆 (journal)
  4) 靈修分享 (faith)
  5) 晨興生活 (morning-revival)
  6) + 新增分類
  7) 修改分類名稱
  8) 刪除分類
輸入編號: 
```

選 1-5（既有分類）或 6（新增分類後）會接著問文章內容、進入「建立新文章」流程；選 7、8 則是分類管理操作，做完就結束，不會接著問文章的事。

#### 建立新文章（選現有分類，或選 6 新增分類）

```
輸入編號: 2
文章標題: 我的新文章
描述 (description): 這篇文章在講什麼
發布日期 (pubDatetime, YYYY-MM-DD) (預設: 2026-07-15): 
封面圖（留空用預設圖；輸入已用過的代稱可重複使用，或輸入圖片路徑新增一張）: ~/Desktop/封面.jpg

已建立文章骨架：src/content/blog/我的新文章.md
去 IDE 把內文寫完就好。
```

跑完之後，去 IDE 打開印出來的路徑，把內文寫完存檔即可，`npm run dev` 會自動偵測更新。若分類是 `faith`，中間會多問 `bookTitle`、`bookAuthor`（這是唯一有額外必填欄位的分類）。

選 6「+ 新增分類」會先問 slug（英文 kebab-case）、導覽列文字、頁面標題、頁面副標、首頁側欄文字，然後自動：

- 在 `src/content/<slug>/` 建資料夾
- 在 [src/content.config.ts](src/content.config.ts) 加一行 `collection(...)` 定義（用跟 journal／morning-revival 一樣的共用 schema，沒有客製欄位）
- 在 [src/config/categories.ts](src/config/categories.ts) 加一筆分類定義
- 自動跑 prettier 定版格式

建完直接接著問文章標題，等於「新增分類＋寫第一篇文章」一次做完。

**限制**：只支援「沿用共用欄位」的分類。如果將來這個分類需要客製欄位（像 faith 的 `bookTitle`/`bookAuthor`、portfolio 的 `tags`/`url`），CLI 不會自動生成，要自己去 `content.config.ts` 幫該分類的 schema 手動 `.extend({...})`。

#### 選 7「修改分類名稱」

```
輸入編號: 7
選擇要修改的分類（自訂路由的分類不列在這裡）：
  1) 作品集 (portfolio)
  2) 隨筆 (journal)
  ...
輸入編號: 2

直接按 Enter 保留原值。
導覽列顯示文字（navLabel） (預設: 隨筆): 生活隨筆
分類頁標題（pageTitle） (預設: 隨筆): 
分類頁副標（pageDesc） (預設: 生活中的隨手記錄。): 
首頁側欄顯示文字（homeDesc） (預設: 隨筆): 
要順便改網址代稱／資料夾名稱嗎？目前是 "journal"，改了網址跟資料夾都會跟著變。 (y/N): n

已更新分類「生活隨筆」。
```

只是想改「顯示文字」（導覽列、分類頁標題等）的話，四個欄位直接按 Enter 跳過不用改的即可，最後網址代稱那題填 `n`，slug／資料夾都不會動，文章網址不會變。如果連網址代稱都想改，最後那題填 `y` 再輸入新 slug，CLI 會把 `src/content/<slug>/` 資料夾整個改名、同步改 `content.config.ts` 裡的變數名稱／路徑，文章網址會跟著變。

#### 選 8「刪除分類」

```
輸入編號: 8
選擇要刪除的分類（自訂路由的分類不列在這裡）：
  1) 作品集 (portfolio)
  2) 隨筆 (journal)
  ...
輸入編號: 2
確定要刪除分類「隨筆 (journal)」嗎？這會修改程式碼檔案。 (y/N): y
該分類目前有 3 篇文章，要一併刪除所有文章嗎？ (y/N): n
要不要先備份這些文章？ (Y/n): y

文章已備份到：.post-backups/journal-1720999999999
已刪除分類 "journal"。
```

流程：

1. 二次確認要不要真的刪。
2. 問要不要連文章一起刪：
   - 填 **y** → 分類定義跟 `src/content/<slug>/` 底下所有文章一起刪掉，刪不回來（沒有備份）。
   - 填 **n** → 文章不會被刪，只會問要不要備份：
     - 備份 → 整個資料夾搬到 `.post-backups/<slug>-<時間戳記>/`（這個資料夾有加進 `.gitignore`，純本機保險用，之後找回來要自己去翻，或用 `git log` 找回原本的 commit）。
     - 不備份 → 文章留在原地 `src/content/<slug>/`，但分類定義已經移除，網站不會再讀取這個資料夾（等於「軟刪除」，之後想要復原就是重新建一個同名分類）。
3. 分類定義（`content.config.ts` + `categories.ts`）一定會被移除，差別只在文章去哪。

**選 7、8 共同的限制**：都只支援「沿用共用欄位」、非自訂路由的分類——有客製欄位的分類（faith、portfolio）或自訂路由的分類（blog）CLI 都拒絕自動處理，選單裡也不會列出來，要手動改。

### 用法二：`npm run post -- <檔案路徑>` — 已經在別的地方寫好 md，要歸檔

適合平常習慣先在別處（例如 Desktop、Notes 匯出）寫好整篇文章，再處理發布這件事的情況。

```
$ npm run post -- ~/Desktop/草稿.md
```

流程：

1. 讀取 frontmatter，檢查 `title` / `description` / `pubDatetime` 是否都有填（faith 分類還會檢查 `bookTitle` / `bookAuthor`）。**缺欄位就直接中止**，不會幫忙腦補內容——回去補齊 md 再重跑一次。
2. 問分類：草稿檔案的上層資料夾名稱如果剛好對到既有分類（例如檔案放在 `.../blog/草稿.md`），會直接建議用那個分類；不然跳出跟用法一一樣的分類選單（含「+ 新增分類」）。
3. 處理封面圖：
   - frontmatter 已有 `coverImage`，而且那個值已經是 `src/assets/covers/` 裡存在的代稱 → 不動它（本來就是有效的重複使用）
   - `coverImage` 指向一個本機圖片路徑（還沒搬進 `src/assets/covers/`）→ 自動複製進去（用來源檔名當代稱），並把欄位改寫成新代稱
   - 完全沒有 `coverImage` → 問一次要不要補（可以直接輸入已有代稱重複使用、或輸入圖片路徑新增一張，留空就跳過、之後 fallback 到預設圖／灰色色塊）
4. 檔名固定用「標題」重新算 slug（不沿用原始檔名），搬到 `src/content/<分類>/<slug>.md`，原始檔案會被移走、不留副本。

> 這個流程的分類選單目前是簡化版（只有既有分類 + 新增分類），沒有修改／刪除分類的選項——那兩個操作請跑 `npm run post`（不帶參數）走用法一的選單。

### 底層檔案結構（交接用，之後要改邏輯去這裡找）

```
scripts/
  post.ts                — 進入點，判斷有沒有帶路徑參數決定跑「新建（含分類管理選單）」還是「發布」流程
  post-lib/
    prompts.ts            — 互動問答（readline + line queue，避免批次貼上答案時漏字）
    categories.ts          — 分類選單（runCategoryMenu，統整選分類/新增/改名/刪除）＋改寫 content.config.ts、categories.ts 的邏輯
    coverImage.ts           — 複製圖片進 src/assets/covers/
    frontmatter.ts            — frontmatter 讀取（gray-matter）／手刻產生／局部欄位改寫
    validate.ts                — 必填欄位檢查表（title/description/pubDatetime + faith 的兩個欄位）
    paths.ts                    — 專案內各路徑常數
```

### 常見狀況

- 檔名衝突（同標題的檔案已存在）：`new` 流程會直接報錯要你換標題，不會覆蓋既有檔案。
- 圖片格式：只接受 `.jpg` `.jpeg` `.png` `.webp` `.avif` `.gif`。
- CLI 的欄位檢查只是「夠用」等級，不是完整複刻 Zod schema 的規則；真正驗證還是要跑一次 `npm run build`（裡面含 `astro check`）。

---

## Q6. 首頁「最新文章」列表的卡片封面圖，大小要怎麼調？

**檔案位置**：[src/components/Card.astro](src/components/Card.astro)

桌面版（`sm:` 以上）的卡片是用「絕對定位＋置中」做出來的：封面圖是獨立於文字欄位的一個置中小方塊，不是撐滿整張卡片左側。跟大小有關的設定分散在 4 個地方，彼此會互相牽動，改的時候要一起看：

**1. 照片本身的顯示尺寸**——`<Image>`（有封面圖，約第 41 行）跟旁邊 fallback 用的灰色色塊 `<div>`（沒封面圖，約第 46 行）上的 `sm:h-* sm:w-*`：

```astro
<!-- 有封面圖 -->
<Image ... class="... sm:aspect-square sm:h-60 sm:w-60" />

<!-- 沒封面圖時 fallback 的灰色色塊 -->
<div class="... sm:aspect-square sm:h-52 sm:w-60" />
```

放大/縮小照片本身，就是改這兩組 `h-*` / `w-*`。**兩邊要填一樣的數字**，不然「有封面圖」跟「沒封面圖」的卡片看起來大小會不一樣（目前這兩行的數字不一致，是測試過程留下的，記得同步）。想維持正方形，同一組裡 `h-*` 跟 `w-*` 也要填一樣的數字——如果高寬分開指定不同數字，`aspect-square` 會被蓋掉，變成長方形而不是正方形。

**2. 照片外層「畫框」的寬度上限**——包住 `<Image>` / 灰色色塊的那層 `<div>`（約第 35 行、第 45 行）上的 `sm:w-*`：

```astro
<div class="... sm:absolute sm:inset-y-0 sm:left-0 sm:flex sm:w-55 sm:items-center sm:justify-center">
```

這是照片可以置中活動的「畫框」寬度，必須**大於等於**第 1 點的照片寬度，不然照片會比畫框還寬，溢出去疊到右邊的文字欄位。

**3. 卡片整體高度上限**——`<li>`（約第 32 行）上的 `sm:h-*`：

```astro
<li class="... sm:relative sm:h-70">
```

邏輯一樣：照片高度不能超過「卡片高度 − 上下留白」，不然會跟下一張卡片的分隔線擠在一起，甚至蓋住下面的「閱讀更多」按鈕。

**4. 文字欄位的左邊留白**——內文 `<div>`（約第 50 行）上的 `sm:pl-*`：

```astro
<div class="min-w-0 overflow-hidden sm:pl-60">
```

要跟第 2 點的畫框寬度同步：`pl-* 的數字 = 畫框寬度數字 + 4`（4 代表 1rem，是照片跟文字中間的間距）。畫框變寬變窄，這裡沒跟著改，文字就會被照片蓋到、或留下不必要的空白。

**調整順序建議**：先改第 1 點的照片尺寸 → 如果超過第 2 點的畫框寬度就把畫框也放大 → 畫框變了，第 4 點的文字留白同步加 4 → 照片變高的話，回頭檢查第 3 點的卡片高度夠不夠。改完存檔，`npm run dev` 開著的分頁會自動重整，直接在瀏覽器上看效果、反覆調整到滿意的比例即可。

---

## Q7. 首頁標題、導覽列下面的分界線、輪播卡片內距，要怎麼調？

**改了什麼**：[src/components/Header.astro](src/components/Header.astro)、[src/components/HeroCarousel.astro](src/components/HeroCarousel.astro)（輪播圖後來被拆成獨立元件，首頁 [src/pages/index.astro](src/pages/index.astro) 現在只是 `<HeroCarousel slides={heroSlides} />` 一行帶進去）

- **站名標題放大**：Header 裡站名 `<a>` 的字級從 `text-3xl sm:text-4xl` 改成 `text-4xl sm:text-5xl`，旁邊的羽毛圖示也同步從 `h-7 sm:h-8` 放大成 `h-8 sm:h-9` 維持比例。標題本來就有 `text-center` 置中，不用另外調。
- **導覽列下面不要再框起來**：原本 `<nav id="nav-menu">` 用 `border rounded-xl`（四邊都有邊框、圓角，看起來像個方框），改成只留 `border-y`（上下兩條線），視覺上變成單純的分隔線，不會把左右兩側包起來。
- **輪播卡片文字太擠**：`HeroCarousel.astro` 裡輪播卡片內層白色區塊的 `max-w-lg` 改成 `max-w-2xl`，桌面版內距 `sm:p-10` 改成 `sm:p-12`，標題跟說明文字有更多呼吸空間，不會擠成好幾行。

**怎麼再調整**：直接改上面這幾個 class 裡的 Tailwind 數值（字級 `text-*`、寬度 `max-w-*`、內距 `p-*`，數字/字級越大代表越大），改完存檔 `npm run dev` 的分頁會自動重整，直接在瀏覽器上看效果。

---

## Q8. 輪播圖切換太生硬（直接跳），還有想要自動輪播，怎麼做？

**改了什麼**：[src/components/HeroCarousel.astro](src/components/HeroCarousel.astro)

原本輪播只有手動點左右箭頭／圓點才會切換，而且切換方式是用 `hidden` class 直接顯示／隱藏，沒有動畫。現在改成：

1. **自動播放**：每 5 秒自動換下一張（`<script>` 裡的 `AUTOPLAY_INTERVAL_MS = 5000`，單位毫秒）。滑鼠移到輪播圖上會暫停，移開後繼續；手動點箭頭／圓點切換時也會重新計時，不會切完馬上又被自動播放蓋掉。
2. **切換動畫改成淡入淡出（crossfade）**：不是直接跳過去，是舊的那張慢慢淡出、新的同時淡入，過渡時間 0.7 秒，靠檔案最後 `<style>` 區塊裡的 `.hero-slide { opacity; transition }` 控制。非目前顯示的投影片會加上 `inert` 屬性，滑鼠點不到、鍵盤 Tab 也不會跳進去。

**怎麼再調整**：

- 想改自動播放間隔：改 `AUTOPLAY_INTERVAL_MS` 這個數字（5000 = 5 秒）。
- 想改淡入淡出速度：改 `<style>` 裡 `.hero-slide` 的 `transition: opacity 0.7s ease;`，數字越大切換越慢越柔和。
- 使用者系統設定「減少動態效果」時，會自動關閉淡入淡出動畫直接切換，不用另外處理。

---

## Q9. 整個網站版面看起來比參考網站窄，怎麼調整寬度？

**背景**：原本 Header（頂部導覽列）跟 Body（首頁輪播圖、側欄、文章列表這些主要內容）用了兩個不同的寬度設定，Header 比較寬、Body 比較窄，兩邊對不齊，Body 看起來特別擁擠。

**改了什麼**：[src/styles/global.css](src/styles/global.css)

```css
@utility app-layout {
  @apply mx-auto w-full max-w-6xl px-4;   /* 頁面整體寬度：從 max-w-4xl 改成跟 Header 一樣寬的 max-w-6xl */
}

@utility max-w-app {
  @apply max-w-4xl;   /* 只控制「文章內文」的閱讀寬度，跟上面這個分開算，維持窄版方便閱讀 */
}
```

連帶把文章內頁（[src/pages/blog/[...slug]/index.astro](src/pages/blog/[...slug]/index.astro)、[src/pages/[category]/[...slug]/index.astro](src/pages/[category]/[...slug]/index.astro)）裡的正文 `<article>` 補上 `mx-auto`——因為正文故意維持窄版（用的是 `max-w-app`），版面整體加寬後如果沒置中，文字會整個貼左邊、右邊留一大塊空白。

**之後想再調整**：

- 想要整體版面更寬／更窄：改 `app-layout` 的 `max-w-6xl`（Tailwind 尺寸依序變寬：`max-w-4xl` < `5xl` < `6xl` < `7xl`）。改這裡記得 `header-layout`（同檔案裡、Header 專用的寬度設定）也要跟著改成一樣的值，Header 跟 Body 才會維持對齊。
- 想要文章正文行寬更寬／更窄（只影響閱讀體驗，不影響整體版面寬度）：改 `max-w-app`，這兩組設定互相獨立，改一個不會動到另一個。

---

## 常用指令

```bash
npm run dev      # 本機開發伺服器，存檔即時預覽
npm run build    # 打包成靜態網站（部署前用來確認沒有錯誤）
npx astro check  # 檢查 Astro 檔案的型別／語法錯誤
npm run post     # 互動選單：建立新文章／新增／修改／刪除分類（帶檔案路徑則改成歸檔已寫好的 md，見 Q5）
```
